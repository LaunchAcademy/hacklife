<h1> Add a Review For This Article </h1>

<%= simple_form_for [@life_hack, @review] do |f| %>
  <%= f.input :title %>
  <%= f.input :body %>
  <%= f.input :rating, collection: 1..5, as: :radio_buttons,
          item_wrapper_class: 'inline' %>
  <%= f.button :submit, 'Submit Review' %>
<% end %>
